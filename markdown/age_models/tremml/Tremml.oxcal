 Plot()
 {
  P_Sequence("Tremml", 1, 0.05, U(-2,2))
  {
   Boundary(start);
   R_Date("TREM-2", 199, 24)
   {
    z=4880;
   };
   R_Date("TREM-1", 149, 21)
   {
    z=4810;
   };
   Date("SURF", N(AD(2000,2)))
   {
    z=N(0,20);
   };
   Boundary(end);
  };
 };
